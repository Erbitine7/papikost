<IfModule mod_rewrite.c>
    RewriteEngine On

    # If this project is served from the parent folder instead of public/, redirect
    # requests into the public/ directory where the front controller lives.
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
